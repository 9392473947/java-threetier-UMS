apiVersion: apps/v1
kind: Deployment
metadata:
  name: umsbackenddeploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: umsbackenddeploy
  template:
    metadata:
      labels:
        app: umsbackenddeploy
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
      - name: umsbackendcon
        image: reddychaithanya06/ums-backend
        securityContext:
            allowPrivilegeEscalation: false
        ports:
        - containerPort: 8081

