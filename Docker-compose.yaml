version: '3.8'

services:

  backend:
    image: optimizedbackendums
    security_opt:
      - no-new-privileges:true
    read_only: true
    ports:
      - "8080:8080"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M

  frontend:
    image: optimizedfrontendums
    security_opt:
      - no-new-privileges:true
    read_only: true
    ports:
      - "80:80"
    restart: unless-stopped
    depends_on:
      - backend

